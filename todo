in add-word, the names for the fields shouldn't be the text that 
is displayed - should have keys that are different from names

DONE adding a word to dict that is already there should populate the form,
submit should update if necessary.  we can do that by setting
the values of the input fields

can we case-fold in a template?  yes, filters.

styles to govern the size of text input fields

delete values that have been cleared on editing

clear button (need javascript)

well-defined ordering for attributes on word-entry pages

capitalize nouns

be well-behaved when there are no more words to test in a quiz
